{
  "name": "preventicus",
  "displayName": "pcoreEditor",
  "description": "A editor to open, manipulate and save pcore files",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:pcoreEditor.openPcoreFile",
    "onCommand:pcoreEditor.doSomethingWithOpenFile",
    "onCommand:pcoreEditor.contextOpenPcore",
    "onCustomEditor:pcoreEditor.editor",
    "workspaceContains:**/*.pcore",
    "onView:pcoreExplorer"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pcoreEditor.openPcoreFile",
        "title": "Open pcore file"
      },
      {
        "command": "pcoreEditor.doSomethingWithOpenFile",
        "title": "Do something with pcore file",
        "icon": "$(arrow-swap)"
      },
      {
        "command": "pcoreEditor.contextOpenPcore",
        "title": "Open with pcore editor"
      }
    ],
    "customEditors": [
      {
        "viewType": "pcoreEditor.editor",
        "displayName": "pcore editor",
        "selector": [
          { "filenamePattern": "*.pcore" },
          { "filenamePattern": "*.pcore.json" }
        ],
        "priority": "default"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "pcoreEditor.contextOpenPcore",
          "group": "navigation",
          "when": "resourceExtname == .pcore || resourceExtname == .json"
        }
      ],
      "editor/title": [
        {
          "command": "pcoreEditor.doSomethingWithOpenFile",
          "group": "navigation",
          "when": "resourceExtname == .pcore || resourceExtname == .json"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "pcore-view-container",
          "title": "pcore editor",
          "icon": "$(heart)"
        }
      ]
    },
    "views": {
      "pcore-view-container": [
        {
          "id": "pcoreExplorer",
          "name": "pcore files",
          "icon": "$(heart)"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "pcoreExplorer",
        "contents": "Get started with the pcore editor!\n\n[Open a pcore or pcore json file](command:pcoreEditor.openPcoreFile)\n\n[More about pcore](command:pcoreEditor.showWelcomeWebview)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.101.0",
    "@typescript-eslint/eslint-plugin": "^8.34.1",
    "@typescript-eslint/parser": "^8.34.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.3",
    "eslint": "^9.29.0",
    "minimatch": "^10.0.3",
    "npm-run-all": "^4.1.5",
    "typescript": "5.8",
    "typescript-eslint": "^8.34.1"
  },
  "dependencies": {
    "@preventicus/pcore": "git+https://github.com/preventicus/pcore_ts.git#FOR-888",
    "glob": "^11.0.3",
    "monaco-editor": "^0.52.2",
    "rimraf": "^6.0.1"
  }
}
